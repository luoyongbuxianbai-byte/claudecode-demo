# 苓桂术甘 · 说话主体与反例状态（120批·用户令九）

**用户令：继续保留候选，不晋升硬门；不必因说话主体未定而停整个工程；
`speaker_status=uncertain` 即可 fail closed。**

## IR 现状（`rules/core_v0.json` → `NEC-LGZG-QICHONG`）

```
proposition       : 苓桂术甘汤有效 ⇒ 有气冲之候
object            : 苓桂术甘汤方证
scope.kind        : formula_pattern
rule_effect       : exclude
validation_status : candidate
source_layer      : L5_editorial
speaker_status    : uncertain        ← ⛔【临床应用】栏体例未界定
text_status       : unresolved
compile_status    : fail_closed      ← ⛔ 依锁7
```

**编译器实测**：`compiler/compile_rules.py` 输出 —— `NEC-LGZG-QICHONG compile=fail_closed`，**未进 runtime**。

## 反例状态（维持 119批 之裁断）

〔传真系·38229 之刘某案〕耳鸣耳聋头晕、苔白脉沉细，判水饮上犯，本方有效，
**记载中六项气冲之候一项未见。**

⛔ **不构成反例**：该案对「气冲」之观察状态为 `unknown_not_recorded`，**不是 `absent_explicit`**。
⇒ IR 内记为 `effect_on_rule: "none"`。

⇒ ⭐ **若据以反证，即犯本工程从 105批 打到 120批 的那个错：「未提及＝阴性推定」。**

## 气冲之候·可观察枚举（原文自带）

心下逆满｜气上冲咽喉｜心下痞硬｜胁下痛｜气上冲胸｜胸胁支满｜起则头眩｜身为振振摇

## 解锁条件

**定【临床应用】栏属胡老还是整理者** → `speaker_status` 由 `uncertain` 变为确值 → 方可重新评估 `compile_status`。
**此问须由通读《经方传真》体例之一线回答，非本线可决。**

# 报告 · 第97批（出题包已就绪 —— 我改任出题者）

## 〇、首行报账

**开工 HEAD `0614d19`｜工作区干净**｜开工四检：语料 **12/12**｜清单 **无减少**｜TAIL **2 处**｜可达性 **51.6%**。

| 文件 | 状态 |
|---|---|
| `tools/blind_pack.py` | **新建**（遮盲＋去标识＋泄漏自检＋sha256 承诺一体） |
| `blind_test/T1–T5.txt` | **新建**·五份遮盲件（含 1 例对照案） |
| `blind_test/新会话执行指令.md` | **新建**·含禁读清单与两阶段规范 |
| `blind_gold/gold_index.json` ＋ `README.md` | **新建**·⛔ 禁读区 |

---

## 一、您这个办法成立，而且比我们建的任何脚本都直接

**污染是会话级的，不是账号级的。** 换一个会话，就是换一个未被污染的执行器。

这一点我和上级都没看到——我们一直在想「怎么建工具防污染」，**而您指出的是「换一个没被污染的人」**。

**我改任出题者，不任答题者。**

---

## 二、三道漏洞已按上级所列逐条堵上

### ① 新会话会去读 gold 文件

→ `blind_test/新会话执行指令.md` 首节即**禁读清单**，逐目录列明。
→ gold **不与遮盲件同目录**：`blind_gold/`（含 README 标明禁读），且**原案正文不复制进去**，只留 sha256 承诺与来源案号。

### ② 引擎里有验案锚，跳读时可能撞见原案

→ **41 例逐例三重指纹核**（病历号／就诊日期／正文中段 18 字短语），比对范围＝新会话可读到的全部语料（引擎＋执行核＋执行件＋白皮书，共 651,272 字）。
→ **三重全 0 且 ≥200 字者 36 例**；所选 5 例全在其中。

### ③ 报告与日志里有答案

→ 禁读清单明列 `reports/`、`holdout/`、`case_layer/`、`term_layer/`、`白皮书/`、`摘录/`、`archive/`、根目录各 `*.md`。
→ 且指令写明：**「若不慎读到禁读项中任何内容，立即停止并声明该例作废，不要假装没看见。」**

---

## 三、选案与遮盲

**T1–T5：4 实测 ＋ 1 对照**（对照案本不需查任何附录；**哪一例是对照，指令中未告知新会话**）。

遮法沿用 `holdout_mask.py` 的**截断法 v4**（该法经 ⑰⑲ 两批实测迭代——词典遮盲已实测失败，因 OCR 把方名写成「小柴朐汤／麻杏惧甘汤」，任何词典都漏，而残留即泄漏）。

**另加去标识**：姓名、日期（含 OCR 坏写如「4966年」）、病历号、**医家名（胡老／冯世纶 → 〔某中医〕，防新会话据以反查原案）**。

### ⚠ 本批自查出三处漏洞，都是我自己的工具造成的，逐条记

| # | 漏洞 | 处理 |
|---|---|---|
| 1 | **泄漏检测器误报**：把「口干思饮」判为方名（「饮」也是症状字） | 收紧为「汤／散／丸」＋「饮子／煎剂」，并排除思／欲／引／渴等前缀 |
| 2 | **T3 尾残留「证为肝郁」**（辨证结论）——CUT 有「证系」无「证为」 | CUT 补「证为」 |
| 3 | **T5 尾残留姓名**——去标识正则要求「男/女＋逗号」，而原文是「男**性**,43岁」 | 补性别变体与句首姓名式 |

**最终泄漏自检：5/5 干净。**

⛔ **这三处若不查，测试当场作废。** 记在这里是因为：**前两批我已经两次被机械核验的漏检咬到**（92批「心下硬满」漏「心下硬」、96b「R83 指针」漏检），**本批是第三次，而这次是我在发出去之前自己查出来的。**

---

## 四、给您的操作步骤（三步）

**第一步（已完成）**：本会话出题，`blind_test/` 与 `blind_gold/` 已 commit。

**第二步（您来做）**：新开一个 Claude Code 会话，**只贴这一句**：

> 读 `blind_test/新会话执行指令.md`，按其执行。

指令里已含：角色说明、禁读清单、两阶段规范（**先无路由判并 commit，再开路由判并 commit，两次分开提交**）、跳读做法、以及「有没有真去读要如实记，不要美化」。

**第三步（回本会话）**：新会话交回两个 commit 后，告诉我一声，**我持 gold 判分**，产出：
- **触发率**
- **被使用率**（真去跳读的／命中触发的）
- **影响结论率**，**分三档：改对／改错／改成不确定**

---

## 五、⛔ 我要先声明这次测试仍有的两个限制

**不等结果出来再说，先说在前面：**

1. **判题者仍是同一模型。** 换会话去掉的是「见过 gold 的记忆」，**去不掉模型本身的先验**——它可能不看路由也能判对某些案，那不算路由无效，也不算路由有效。**这个数只能测「路由改变了什么」，不能测「引擎有没有用」。**
2. **五例太少。** 触发率、被使用率在 5 例上都是粗估，**任何小数点后的比较都无意义**。我判分时只报整数比例与绝对条数。

---

## 六、上级五、六两条已受理

- **五**：哈希承诺脚本已建（`blind_pack.py` 内 sha256 承诺），**并入本次基建，即刻用于本测**。
- **六**：「机械核验之结果须先验其漏检」**扩入闸门9 第九款**——本批第三次被它咬到，这次自查捞出。

---

## 七、上级「硬线」我接受，并自陈一句

上级指出我近两批重心已由引擎内容转向测试基建（改名／路由／核验工具／哈希脚本），**必要但不增加辨证能力**，再走两批就会变成「为了测量而测量」。

**这条批评我接受。** 出题包做完即止。

**本批之后：回到内容——十二书重跑 ＋ 附录E 甲族 1,412 条真／伪二分。** 盲测结果回来后我只做判分，**不再为测量单独立批**。

---

## 八、未做 / 口径之界

- **被使用率、影响结论率仍未产出**（待新会话交回）。
- 现存一切分数仍是九书口径——**这是下一批要还的账**。
- 显性欠账不变：条文挂载 161 条、1448 条两栏补标、12 格全谱、食毒证据谱、C卷主干替换。
- 本批**未产出任何新条款**（暂停令同守）。

# 报告 · 第120批（Typed IR 执行链建成；而 §172 那条反例还牵出胡老自身两读）

## 〇、HEAD / corpus / manifest / fresh checks

**开工 HEAD `9b9cdaf`**｜语料 **12/12**｜清单 **✅无减少**｜新鲜度 **26/26**｜lint **✅0 错 0 警**。

| 交付 | 真实路径 |
|---|---|
| Typed IR 规范 | `docs/RULE_SCHEMA_V0.md` |
| JSON Schema | `schema/rule_v0.json` |
| IR 规则 | `rules/core_v0.json`（5 条） |
| 编译器 | `compiler/compile_rules.py`｜`compiler/lint_rules.py` |
| 参考执行器 | `runtime/reference_evaluator.py`｜`runtime/compiled_rules.json` |
| 依赖审计 | `term_layer/P0真实下游依赖审计_120批.md` |
| 复合前件复核 | `term_layer/P0复合前件合法性复核.md` |
| 太阳提纲矩阵 | `term_layer/太阳提纲_作用域与反例矩阵.md` |
| §70 审计 | `term_layer/§70_病程适用域审计.md` |
| 苓桂术甘 | `term_layer/苓桂术甘_说话主体与反例状态.md` |
| 版本规范 | `term_layer/版本与source_layer规范.md` |
| 测试 | `tests/` 共 **11 个套件**（含 T1–T20 与令十六所列七件） |
| 依赖审计工具 | `tools/yilai_shenji_120.py` |

---

## 一、本批发现的新错误

### ⭐⭐ 1 §172 之外，**胡老自己在同一条文上有两读**——本批新发现

您给的反例〔讲伤寒·**188178**〕已逐字核实：
> 「这个太阳病的证候也不过是脉浮，发热而已。**它并不恶寒**」

⭐ **而查证时撞到另一处**：**同一 §172**，胡老在另一本书的【注解】里写：
〔A·C卷·**125102**〕
> 「**太阳病之发热恶寒**与少阳病之口苦咽干同时出现，故谓太阳与少阳合病」

⇒ ⛔ **《讲伤寒》说「它并不恶寒」，《经方理论与实践》说「太阳病之发热恶寒」——同条两读。**

⇒ **这不削弱撤销，反而加强**：一个可信反例已足以证伪全称式；
**而此处连原作者自己都不一致，则该全称式更不可作硬门。**
⇒ 依令八：**两造皆挂，`text_status = disputed`，不删其一。**

### ⛔ 2 我自己的测试又踩了同型坑（第三次）

`T13c`「evaluator 内无 `support_count>=N` 升级式」——**初版直接扫源码文本，
被【声明该禁令的注释本身】命中而假红**。注释里正写着「不存在 support_count >= N → confirmed」。
⇒ 已改为**先剥注释再扫**。**这是 118批 C2 假绿、119批 A2/A15 之后的第三次同型。**

### ⛔ 3 L3658 带 `[推演]` 标记却在执行层作 confirm

「腹满嗳腐+便溏 → 保和丸**`[推演]`**」——**行内自带 `[推演]`，即非胡老原文**，
`source_layer=L6_engine`，**依锁7 须 fail_closed**。

### ⛔ 4 L4111 自称「充分判据」而无反例攻击记录

「喜按（实纲/虚纲之**充分判据**）」——`validation_status` 不足。

### ⛔ 5 L3078 之 scope 混层

「口不渴★ ∧ 四肢不温★ ∧ 下利清谷★ → 热纲否」——其中「下利清谷」之适用域
119批 已判为 §91 **病程条件**，**`stage_conditional` 成分混入 `cross_scenario` 排除式**。

---

## 二、撤销「太阳病 ⇒ 恶寒」全局硬门（令一）

**IR**：`rules/core_v0.json` → `NEC-TAIYANG-WUHAN`
```
validation_status : falsified_overbroad
compile_status    : inactive
counterexamples   : 讲伤寒·188178(falsifies) ＋ C卷·125102(disputes)
text_status       : disputed
```
**编译器实测**：`NEC-TAIYANG-WUHAN compile=inactive` → **未编译入 runtime**（非仅改措辞）。

**并撤销 119批 之声明**：~~「本工程第一条完成四条件的必要条件」~~。
**现行状态**：`反例攻击：完成｜攻击结果：D5 全局必要关系被证伪｜后续：scope split required`。

**作用域拆分**（A 独立太阳提纲型／B 病例中太阳成分／C 合病并病中太阳成分）已建表，
**三者现无一条经反例攻击存活**。⛔ 遵令**未以 `pure=true` 标签救活旧规则**。

**最多保留者**：`SUP-WUHAN-BIAOYANG`——`恶寒 present → support(表位/太阳)`，`compile_status=support_only`。

---

## 三、P0 16 项真实依赖审计（令二·改账）

⛔ **119批 之矛盾已认**：一面「86 中已读 20」，一面验收写「16 项已完成 ✅」。**该 ✅ 撤销。**

| 项 | 数 |
|---|---:|
| 机器收窄候选 | **76** |
| **human_read** | **76** |
| ⛔**unknown_due_to_not_read** | **0** |

⚠ **119批 报 86，本批 76**——差额系 119批 自身之改动给若干行加了撤销标记，**已自动移出候选集，非漏计**。

**actual_dependency 七值分布**：`true_dependency` 55｜`commentary` 18｜`case_text` 2｜`duplicate` 1。
**validation_status 分布**：`source_verified` 44｜`candidate` 28｜`scope_verified` 4。

⇒ ⭐ **`candidate` 28 条＝尚未达执行阈值者。依锁1，它们不得以 confirm/exclude 之效力 active。**

---

## 四、复合前件合法性复核（令三、令十五）

⛔ **新增硬不等式 `compound_antecedent != validated_rule` 已入规范。
119批「✅复合前件·本就合法·保留」之 reason 全部撤销。**

**23 条复核结果**：

| 最终类型 | 条 |
|---|---:|
| `support_only` | 14 |
| `trigger_only` | 6 |
| `disputed` | 2 |
| `confirmed_rule` | **1** |

⇒ ⛔ **119批 判为「合法·保留」之 11 条，复核后无一条为 `confirmed_rule`**——
全部降为 `support_only` / `trigger_only` / `disputed`。
⇒ **理由不是它们错，是【未做同域反例攻击】⇒ `validation=candidate` ⇒ 依锁1 不得 confirm。**

---

## 五、五维拆分（令四）

```
rule_effect       trigger｜support｜confirm｜exclude｜contraindicate
validation_status candidate｜source_verified｜scope_verified｜counterexample_audited｜
                  disputed｜rejected｜falsified_overbroad
observation_state present｜absent_explicit｜unknown_not_asked｜unknown_not_recorded｜uncertain
runtime_status    candidate｜confirmed｜excluded｜undetermined
source_layer      L1_fact｜L2_hu_behavior｜L3_response｜L4_hu_theory｜L5_editorial｜L6_engine
```
**并附** `speaker_status`（5 值）与 `text_status`（5 值）。

⛔ **锁1 实装**：IR **不得携带 `runtime_status` 字段**（lint 报错）；
`compile_status=active` 须 `validation ∈ {scope_verified, counterexample_audited}`。
**`source_verified` 绝不自动产生 `runtime_status=confirmed`。**

**删除**：119批 之「两个 supporting_evidence 可解除七禁」——**`evaluator` 代码内无任何聚合升级式**，
T13/T13c 已断言（support×2、×100 皆 `final_confirmed_count=0`）。

---

## 六、absent_explicit 排除逻辑（令六）＋ §70 scope（令七）

**absent_explicit 不再是天然 exclusion operator**：
```
absence_can_exclude IFF
  存在 validated_scoped_necessary_rule (Y⇒X) ∧ observation=absent_explicit ∧ scope_match
```
T14/T14b、`tests/test_absence_requires_necessary_rule.py`（7 项）已断言。

**§70 恢复 `stage_conditional` scope**：`treatment_history.includes(发汗)`。
- **T15**：无发汗史 → `S70-HANHOU-SHI skip — scope 条件未满足`，`confirmed=0` ✅
- **T15b**：有发汗史 → `confirmed=['实纲/阳明里实']` ✅

⚠ **全局调用点检查之范围须声明**：只覆盖执行层 76 行，**06 层历史记录未逐行扫**——
**故是「已检索范围内未查得」，不是「全库无」。**

---

## 七、Typed IR 执行链（令十、十一、十二）

```
rules/*.json ──lint_rules.py（7 锁）──▶ compile_rules.py ──▶ runtime/compiled_rules.json
                                                                      │
                                                        reference_evaluator.py ──▶ case trace
```

**编译实测**：5 条 IR → **入 runtime 2 条**（`SUP-WUHAN-BIAOYANG` support_only、`S70-HANHOU-SHI` active）；
**未编译 3 条**（`NEC-TAIYANG-WUHAN` inactive｜`NEC-LGZG-QICHONG` fail_closed｜`JUEYIN-SHANGREXIAHAN` inactive）。

⛔ **legacy 默认 fail closed 已实装**：**evaluator 只吃 `compiled_rules.json`，Markdown 一律不进 runtime。**

**evaluator 真实读取**：`rule_effect`／`validation_status`／`scope`／`observation_state`／`compile_status`，
并产出 `runtime_status`。

---

## 八、静态测试结果

| 套件 | 结果 |
|---|---|
| `tools/yuyi_huigui.py`（文本语义 A/B/C/D） | **15/15** |
| `tests/test_rule_schema.py` | **5/5** |
| `tests/test_fail_closed.py` | **5/5** |
| `compiler/lint_rules.py` | **0 错 0 警** |

## 九、执行语义测试结果（reference runtime）

| 套件 | 结果 |
|---|---|
| `tests/test_reference_runtime_T1_T20.py` | **36/36** |
| `tests/test_support_never_auto_confirms.py` | **4/4** |
| `tests/test_absence_requires_necessary_rule.py` | **7/7** |
| `tests/test_composite_scope.py` | **3/3** |
| `tests/test_validation_runtime_orthogonal.py` | **5/5** |
| `tests/test_version_conflict.py` | **5/5** |
| `tests/test_assertion_type_semantics.py` | **21/21** |
| `tests/test_unknown_semantics.py` | **9/9** |
| `tests/test_p0_propagation.py` | **14/14** |

**11 个套件全绿。**

⛔⛔ **措辞界限**：允许报 `reference runtime semantic closure = PASS`；
**禁止报** `LLM 实际阅读 Markdown 行为已验证 = PASS`。

---

## 十、仍失败项目

**无 FAIL。**

## 十一、未做事项

1. **A/B/C 三种太阳作用域之可观察边界未定义** ⇒ 不得建规则（遵令，本批不做）。
2. **苓桂术甘之【临床应用】栏层级未定** ⇒ 维持 `fail_closed`。须通读体例之一线回答。
3. **`source_version` 多为空**；`revision_relation` 只填了一条。
4. **06 层历史记录未逐行扫**（§70 调用点检查之范围限制）。
5. **28 条 `validation=candidate` 未做同域反例攻击**——这是下一步最大的人工任务。
6. 遵令十七：未新增六经本体规则／未扩 272 药功能对／未扩三毒／未扩位性全谱／未增方证卡／未跑盲测。

---

## 十二、是否达到 P0 闭环验收条件

**令十八之 22 项，逐条核**：

| # | 条件 | 判 |
|---|---|---|
| 1 | §172 反例已进入规则资产 | ✅ `counterexamples[0]` |
| 2 | 太阳病⇒恶寒 D5 硬门已撤 | ✅ `falsified_overbroad` + `inactive` |
| 3 | 无恶寒不再全局排太阳成分 | ✅ T11 |
| 4 | 86/86 依赖候选全部人读 | ✅ **76/76**（口径已说明） |
| 5 | unknown_due_to_not_read = 0 | ✅ |
| 6 | 复合前件合法项完成二次核证 | ✅ 23 条 |
| 7 | rule_effect 与 validation_status 拆开 | ✅ |
| 8 | observation_state 独立 | ✅ |
| 9 | runtime_status 独立 | ✅（IR 禁携带，lint 拦） |
| 10 | source_layer 独立 | ✅ |
| 11 | text_status 独立 | ✅ |
| 12 | 两 support 自动升级规则已删 | ✅ |
| 13 | support_count 永不直接产生 confirm | ✅ T13/T13c |
| 14 | absent_explicit 只能经 scoped necessary rule 排除 | ✅ T14 |
| 15 | §70 恢复「发汗后」scope | ✅ T15/T15b |
| 16 | L5 不得覆盖 L4/L2 | ✅ 锁5 + T19 |
| 17 | Typed IR schema 建立 | ✅ |
| 18 | legacy 默认 fail closed | ✅ T16 |
| 19 | compiler 只编译合法 IR | ✅ |
| 20 | reference evaluator 真实读取 IR 各字段 | ✅ |
| 21 | T1–T20 全部通过 | ✅ 36/36 |
| 22 | 所有剩余 NOT_TESTABLE 明确列出 | ✅ 见下 |

### ⇒ **`P0 reference-runtime semantic closure = PASS`**

⛔ **此句之界限（不可略）**：它只说
**「在 reference runtime 上，语义差异真实生效」**。
**它不说 LLM 读那份 20,574 行 Markdown 时会照此执行——那仍是 NOT_TESTABLE。**

---

```
STATIC_SCHEMA        : PASS（lint 0 错 0 警｜schema 5/5｜fail_closed 5/5｜文本语义 15/15）
DEPENDENCY_AUDIT     : 76 / 76 human-read（unknown_due_to_not_read = 0）
REFERENCE_RUNTIME    : T1–T20 全部通过（断言 36/36）；11 套件合计 109/109

FALSIFIED_RULES      : 1  NEC-TAIYANG-WUHAN（太阳病⇒恶寒·D5 全局）
                          —— 反例 讲伤寒·188178(§172)

DISPUTED             : 3  §172 之太阳成分是否恶寒（胡老自身两读）
                          厥阴⇒上热下寒（L5 强化 vs L4 结构）
                          柴胡桂枝干姜汤归经（早期少阳 vs 后期厥阴·编者自承）

UNKNOWN              : 5  太阳三征之「头项强痛」必要性
                          A/B/C 三种太阳作用域之可观察边界
                          苓桂术甘【临床应用】栏之层级
                          28 条 validation=candidate 之同域反例
                          06 层历史记录（§70 调用点检查范围外）

NOT_TESTABLE         : 1  LLM 自由阅读 Markdown 是否严格执行 Typed IR 语义
```

⛔ **「反例攻击完成」与「规则经攻击后仍存活」已分开统计**：
本批**反例攻击完成 2 条**（太阳恶寒、苓桂术甘）；
**攻击后仍存活者 0 条**——一条被证伪，一条卡在说话主体未定。

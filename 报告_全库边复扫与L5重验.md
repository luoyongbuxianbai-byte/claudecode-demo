# 批次⑦报告·全库边复扫 ＋ L4/L5 重验

**【在途·未完项见文末｜其中 L4 未能执行，原因见第四节】**
引擎commit `a9d9d0c` `335ba79` `cecdd29` ／ 治理commit `2719467` ／ 本报告commit

---

## 一、复扫查出的根本缺陷：我在①-⑤批违反了协议8③

协议8③写的是"今后新增鉴别强制双挂"。批次①-⑤新建的全部鉴别知识，**都只写进了
附录X谱系表，没有回挂到方证条目**。

后果比"单向"更重：协议8原本要修的是"判据只挂gold方、对已锚定误配方的执行器不可见"，
而这批边是**整个不在任何条目上**——无论执行器锚到哪张方，都看不见。实测：

> 回挂前，附录X 谱系区的边有 **94.2%** 在方证条目层完全不可见。

这不是新缺陷，是我把协议8刚修好的洞按原样又挖了一遍。

## 二、回挂做法与指标

对 61 条附录X分野注**逐条人工判读**，排除 5 条不构成鉴别关系的（`◆其他◆`、
`◆其他章◆` 两个纯归档桶；`◆本批新增判据类型◆` 元注；`◆防己支◆` 归档说明；
`◆补挂·大建中汤◆` 挂载头，其边实载于后两段）。**没有用关键词正则筛**——先试过，
两个方向都错：把 `◆桂枝去芍药支(去芍=去腹满痛之治)◆` 这类真分野漏掉，
又把 `◆其他◆` 这类纯归档桶收进来。

据余下 56 条，给 86 个原本无出口的条目补挂出口指针行（载明附录X锚 ＋ 分野名 ＋ 对举方名）。

| 指标 | 回挂前 | 回挂后 |
|---|---|---|
| 出口覆盖率（306条目中有出口者） | 19.3%（59） | **47.4%（145）** |
| 互指完备率（有向边中双向者） | 12.9%（170边/11对） | **47.6%（525边/125对）** |
| 附录X边中条目层不可见者 | 94.2% | 55.0% |
| 全库边条目可见率 | 58.5% | **85.8%** |

**关于与 2026-07-26 基线（22.5%／20.5%）的可比性**：那版扫描器已随容器回收丢失，
本次是重建的扫描器，口径未必一致，**两组数不可直接比较**。上表前后两列出自同一
扫描器，可比。

**扫描器自验**（"指标脚本本身须验证"，W-1.12 同型）：内置断言，检验
`当归四逆汤` 不会因子串被误记成一条指向 `四逆汤` 的边（最长优先消费）。

## 三、自查捕获的一处真实回归

回挂做完、锚校验通过之后，我又在附录X**之前**插入了 L5 复核文字（16行）和
"锚的读法"说明（8行）——**106 个绝对行号锚随即全部失准**。写那条说明的编辑本身
就再次触发了它要说明的问题。

**关键点是它怎么被发现的**：只查"锚是否指向一个 ◆ 行"会**全部通过**——移位之后
落在的仍然是别的 ◆ 行。是"对举方是否确在被引段内"这一步把它逼出来的（366/368 失败）。

> **校验必须查内容归属，不能只查格式。**

处置：①附录X头部写明"行号只是跳转提示，**◆段名◆才是权威锚**，不一致以段名为准"；
②给两个长标题段补规范短段名，使 106 个锚全部可按段名解析；
③新建 `tools/check_backmount.py`（入库，非scratchpad孤本），校验锚落点／对举方成员／
条目归属三项，`--fix` 可按段名重算行号。

最终校验：**回挂引用 106 ｜ 锚失准 0 ｜ 对举方缺失 0（368个引用全部落实）｜ 挂错方 0**。

## 四、L4 未能执行——原因不是技术，是语料枯竭

核查 C卷 107 个带验案的方，其中 **92 个的引擎 ⑤呈现校准明标"由该案提炼"**。
也就是说，**C卷 111 则验案已被 X-0 全数消化进引擎**。

由此：

- 此刻跑"全量守卫复跑"，测到的是**训练集召回率，不是泛化能力**。数字会很好看，
  但不说明任何事。
- 守卫 G5-G12 本身也一样——它们**作为回归/漂移检测仍然有效**（那是守卫的本职：
  检测引擎改动有没有让原本对的变错），**但不能拿来当准确率证据**。这两个用途
  必须分开记。
- **我自己更不能跑**：金标准我全知道，此前已就此自陈过一次。

**有效的留出集必须来自第二期矿脉**（ocr1 的另 216 条辨证要点、其余 OCR 各书的验案）。
在那之前，L4 的"泛化准确率"一栏应当是空的，不应当填一个漂亮的数。

我没有为了交差去跑一个我知道无效的测试。

## 五、L5 温病靶复核：两缺口未闭合，但成因精确化了

**乙（湿温·湿热无黄疸）**：逐条复查了 ★库中全部"热而不必发黄"的方——
白头翁汤（热利下重）、黄芩汤（太少合病下利）、黄连阿胶汤（心烦不得卧）、
栀子豉汤（虚烦懊侬）、猪苓汤（渴＋小便不利＋发热）、甘草泻心汤·狐惑（蚀于喉/阴）——
**无一以"湿"为★成分**：或以**下利**为必要，或纯属**燥热/虚热**，或以**溃疡**定型。

> 结论：`身热不扬＋身重＋口黏＋苔腻＋脉濡` 这一组合，**在经方★库中确无对应完整
> 方证**，非誊写不足所致。

唯一近似的是**三物黄芩汤**★"里热**血热**见心烦、手足心热者"，但①属血热非湿热，
②该条目现带"DDL层级未核定·执行器不得凭此块单独出方"标记，本身还不可用于匹配。

**丙（营血分样）**：X-0 为★库新增了本库此前没有的**血热一路**（三物黄芩汤，方解
"生地用量独多，故尤宜于有发热心烦之血证"）。但其★**不含斑疹项**。
缺口成因因此由"全库无血热方"精确为"**有血热方而无斑疹项**"——记这个差别，是因为
两者对后续补强的指向完全不同（前者要去找方，后者要去查是否另有带斑疹项的★）。

两处**均维持登记**：不得强锚升麻鳖甲汤，不得以温病方补入（违R1闭卷）。

## 六、顺带查出：14 个条目是"死"的

全库有 **14 个 X-0 缺口新增条目**带着 `DDL层级未核定·交Code裁决·执行器不得凭此块
单独出方` 标记——它们在引擎里，但**不能参与匹配**。三物黄芩汤（L5 乙缺口的唯一
近似候选）正是其中之一。

这 14 个的 DDL 裁定是我的活。**本批没做**：DDL 层级是病位深度判定，需要逐方按
原文认真判，不适合当作长批次的尾巴匆匆带过。已登记为独立待办。

---

## 未完项清单（在途）

1. **L4 全量守卫复跑**：**无法有效执行**（语料枯竭，见第四节）。须待第二期矿脉
   提供留出集。当前守卫仅可用作回归/漂移检测。
2. **14 个条目 DDL 层级待裁**（含三物黄芩汤，直接卡着 L5 乙缺口的候选资格）。
3. **仍有 161 个条目无出口**（306 中）。其中未被任何附录X分野注覆盖的，须另找边源。
4. **附录X 边中仍有 55% 未回挂到条目**——本批只回挂了"该条目完全无出口"的情形；
   已有出口但缺这条边的条目未处理。
5. **呈现型改写（⑥批16处）是否造成判据漂移，仍未复验**——本要靠 L4，现顺延。
6. **取证靶未重跑**（附录Y 修订后）。
7. **X-0 六段齐取在全库尺度未完成**：现完成的是族级谱系层（①方解②辨证要点），
   ⑤呈现校准 217 段中 109 段标注"原书本方无验案"。
8. **第二期矿脉未启**：ocr1 的 216 条增量辨证要点；C卷绪论理论段 vs 纲领一~六 对照。

---

**待触发：下一批 = ⑧ 竣工报告**
